<%= form_tag search_round_path, remote: true, method: :get, id: "submission-round-form" do %>
  <%= text_field_tag :search_param, params[:search_param] %>
  <%= button_tag(type: :submit, class: "btn btn-lg btn-success") do %>
           <i class="fa fa-search"></i> "Guessing Round"
         <% end %>
<% end %>
